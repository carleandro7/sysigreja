<%= form_with(model: igreja) do |form| %>
  <% if igreja.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(igreja.errors.count, "error") %> prohibited this igreja from being saved:</h2>

      <ul>
        <% igreja.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nome, style: "display: block" %>
    <%= form.text_field :nome %>
  </div>

  <div>
    <%= form.label :distrito_id, style: "display: block" %>
    <%= form.text_field :distrito_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

json.extract! visitaigreja, :id, :nome, :apelido, :telefone, :whatsapp_telefone, :cep, :logradouro, :numero, :bairro, :cidade, :estado, :latitude, :longitude, :observacao, :igreja_id, :created_at, :updated_at
json.url visitaigreja_url(visitaigreja, format: :json)
